[package]
name = "smtkit-core"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Solver-agnostic typed IR for SMT constraints (core)"
publish = true

[dependencies]
thiserror = "2"

[features]
default = []

